# Framework Documentation

This directory is for developing the ResearchKit template framework.

- `adr/`: architecture decisions that affect repository shape or runtime design.
- `handoffs/`: session handoff documents and factual context from prior work.
- `plans/`: implementation plans for framework changes.

Consumer-facing research workflow documentation belongs in `templates/ara-research-workspace/docs/`.
